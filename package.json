{
  "name": "docs",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "mint dev --port 3002",
    "build": "mint build",
    "lint": "mint broken-links",
    "check-types": "mint broken-links"
  },
  "devDependencies": {
    "mint": "latest"
  }
}
