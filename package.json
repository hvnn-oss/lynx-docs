{
  "name": "docs",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "mint dev --port 3002",
    "build": "mint validate",
    "lint": "mint broken-links",
    "check-types": "mint broken-links"
  },
  "devDependencies": {
    "mint": "latest"
  }
}
